<% @comments.each do |comment| %>
    <h3>Comment belongs to the post:</h3> <%= link_to comment.course.title, course_path(comment.course) %><br>
    <h3>Comment content:</h3> <%= comment.content %>
<% end %>